<article>
    <slot />
</article>
<style is:global>
article {
	color: var(--black-080);
    display: flex;
	flex-direction: row;
    align-items: flex-start;
    gap: 1rem;
}
article > * {
    background-color: var(--stickie-yellow);
    padding: 1rem;
    border-radius: 0.25rem;
    width: 16rem;
    aspect-ratio: 1 / 1;
    box-shadow: var(--stickie-shadow);
    outline: var(--contour);
}
article > * > p + p,
article > * > p + h4,
article > * > ul + p {
    margin-top: 1rem;
}
article > * > p:last-child {
    margin-bottom: 0 !important;
}
article p {
    position: relative;
    overflow: hidden;
}
article img {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
}
[tags] ul {
	list-style-type: none;
	display: flex;
	flex-flow: row wrap;
	align-items: flex-start;
	gap: 0.5em;
	padding: 0.5em 0;
}
[tags] li {
	line-height: 125%;
    padding: 0.125em 0.25em;
	background-color: var(--white-medium);
	border-radius: 0.125em;
	white-space: nowrap;
}
</style>