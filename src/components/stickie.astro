<article>
    <slot />
</article>
<style is:global>
article {
	color: var(--black-080);
    display: flex;
	flex-direction: row;
    align-items: flex-start;
    gap: 1rem;
}
article > * {
    background-color: var(--stickie-yellow);
    padding: 1rem;
    border-radius: var(--sd);
    width: 15rem;
    aspect-ratio: 1 / 1;
    box-shadow: var(--shadow-a);
}
article > * > p + p,
article > * > p + h4,
article > * > ul + p {
    margin-top: 1rem;
}
article > * > p:last-child {
    margin-bottom: 0;
}
article > p {
    position: relative;
}
article img {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: var(--sd);
}
article img + small {
    position: absolute;
    bottom: 0;
    left: 0;
    border-radius: var(--sd);
    box-sizing: border-box;
    width: 100%;
    max-height: 100%;
    padding: 3rem 1rem 1rem;
    color: var(--paper);
    background: linear-gradient(to top, var(--black-strong) calc(100% - 4rem), transparent calc(100% - 0.25rem));
}
</style>