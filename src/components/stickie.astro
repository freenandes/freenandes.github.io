<article>
    <slot />
</article>
<style is:global>
article {
	color: var(--black-080);
    display: flex;
	flex-direction: row;
    align-items: flex-start;
    gap: 1rem;
}
article > * {
    background-color: var(--stickie-yellow);
    padding: 1rem;
    border-radius: 0.25rem;
    width: 16rem;
    aspect-ratio: 1 / 1;
	box-shadow: rgba(50, 50, 93, 0.125) 0px 2px 5px -1px, rgba(0, 0, 0, 0.15) 0px 1px 3px -1px;
	overflow: hidden;
}
article > * > p {
    margin-bottom: 1.5rem;
}
article > p:has(img) {
    position: relative;
}
article img {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
}
</style>