<article id={Astro.props.id}>
    <slot />
</article>
<style is:global>
article {
	color: var(--black-strong);
    display: flex;
	flex-direction: row;
    flex-wrap: wrap;
    align-items: flex-start;
    gap: 0.5rem 1rem;
}
article > * {
    box-sizing: border-box;
    background-color: var(--stickie-yellow);
    width: 17rem;
    padding: 1rem;
    border-radius: var(--sd);
    aspect-ratio: 1 / 1;
    box-shadow: var(--shadow-a);
}
article > ul {
    padding-left: 2em;
}
article > * > p + p,
article > * > p + h4,
article > * > ul + p {
    margin-top: 1rem;
}
article > * > p:last-child {
    margin-bottom: 0;
}
article > p {
    position: relative;
}
article img,
article video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: var(--sd);
}
article img + small,
article video + small {
    font-size: 0.625em;
    position: absolute;
    bottom: 0;
    left: 0;
    border-radius: var(--sd);
    box-sizing: border-box;
    width: 100%;
    max-height: 100%;
    padding: 4rem 1rem 1rem;
    color: var(--paper);
    background: linear-gradient(to top, var(--black-strong) calc(100% - 8rem), transparent calc(100% - var(--sd)));
    transition: opacity 1s ease-in-out;
    z-index: 1;
    pointer-events: none;
}
article img:hover + small,
article video:hover + small {
    opacity: 0;
}
article > hr {
    flex-basis: 100%;
    box-sizing: border-box;
    width: auto;
    height: 0;
    padding: 0;
    margin: 0;
    border: none;
}
article > hr + * {
    margin-left: 18rem;
}
/* Content design */
#wat-06 {
    width: var(--maxcol-04);
}
#wat-05 {
    width: var(--maxcol-05);
}
#wat-07,
#wat-08 {
    width: var(--maxcol-06);
}
#wat-04 {
    width: var(--maxcol-07);
}
</style>